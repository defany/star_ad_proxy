services:
  app:
    build:
      context: .
      dockerfile: app.Dockerfile
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
    ports:
      - "8080:8080"
    restart: unless-stopped
